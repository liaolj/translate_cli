# Copy this file to `.env` and fill in the values before running the CLI.
# The API key can be generated in your OpenRouter dashboard.
OPENROUTER_API_KEY=your_api_key_here

# Configure which OpenRouter model to use for translation, for example:
# - openrouter/auto
# - openai/gpt-4o-mini
# - anthropic/claude-3-haiku
MODEL=openrouter/auto

# Optional overrides
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# OPENROUTER_SITE_URL=https://your-app-domain
# OPENROUTER_APP_NAME=translate-cli
# TRANSFOLD_SPLIT_THRESHOLD=8000  # maximum characters before Transfold splits a document
# DATA_ROOT=/path/to/store/translations  # default: var/transfold
